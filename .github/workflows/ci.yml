name: My first pipeline

on: push
    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: uptime
        run: uptime
    
      - name: Number of processors
        run: npric   
  test:
    runs-on: ubuntu-latest
    steps:
      - name: hello world
        run: echoHello world"